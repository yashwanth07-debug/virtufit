# ---------------------------------------------------------------------------
# TryOn Studio — frontend-only micro product
#
# PRIMARY: YouCam API key (cloth-v3 virtual try-on, ~2 units/success).
# Get one: https://yce.makeupar.com/api-console  — every hackathon
# participant gets 1,000 free units. YouCam's API is CORS-enabled, so the
# browser calls it DIRECTLY — no backend, nothing stored.
VITE_YOUCAM_KEY=

